services:
  paperless-graphrag:
    build: .
    container_name: paperless-graphrag
    ports:
      - "8003:80"
    volumes:
      # Persist GraphRAG data and sync state
      - ./data:/app/data
    environment:
      # Paperless-ngx connection
      - PAPERLESS_URL=${PAPERLESS_URL:-http://paperless:8000}
      - PAPERLESS_TOKEN=${PAPERLESS_TOKEN}

      # LiteLLM connection
      - LITELLM_BASE_URL=${LITELLM_BASE_URL:-http://litellm:4000}
      - LITELLM_API_KEY=${LITELLM_API_KEY}

      # Model selection is managed via UI settings page (runtime_settings.json)
      # Only set these if you want to override UI-managed settings:
      # - INDEXING_MODEL=${INDEXING_MODEL}
      # - QUERY_MODEL=${QUERY_MODEL}
      # - EMBEDDING_MODEL=${EMBEDDING_MODEL}

      # GraphRAG settings (managed via UI settings page)
      # Chunk size/overlap are stored in runtime_settings.json

      # Rate limiting (managed via UI settings page)
      # Stored in runtime_settings.json

      # Database for persistent chat history and AI processing
      - DATABASE_URL=${DATABASE_URL:-}
    networks:
      - app_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      kuma.paperless-graphrag.http.name: "Paperless GraphRAG"
      kuma.paperless-graphrag.http.url: "http://10.10.10.10:8003"

networks:
  app_network:
    name: ${DOCKER_NETWORK:-paperless-graphrag-net}
    external: ${DOCKER_NETWORK_EXTERNAL:-false}
